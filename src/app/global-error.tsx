"use client"; // Error boundaries must be client components

import ErrorReporter from "@/components/ErrorReporter"; // Import the ErrorReporter component

export default ErrorReporter; // Re-export ErrorReporter as the default export
